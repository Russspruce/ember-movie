import { moviePopularity } from 'ember-extended/helpers/movie-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | movie popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = moviePopularity([42]);
  assert.ok(result);
});
